<Window x:Class="AccommodationSystem.Views.AdminLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="管理画面ログイン" Width="360" Height="340"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Border Margin="20" Style="{StaticResource CardBorder}">
        <StackPanel>
            <TextBlock Text="🔐 管理画面" FontSize="20" FontWeight="Bold"
                       Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,16"
                       HorizontalAlignment="Center"/>

            <TextBlock Text="パスワード" Style="{StaticResource FieldLabel}"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,8"
                         KeyDown="PasswordBox_KeyDown"/>
            <TextBlock x:Name="ErrorText" Foreground="{StaticResource DangerBrush}"
                       FontSize="12" Margin="0,0,0,16" Text="" TextWrapping="Wrap"/>

            <Button x:Name="LoginButton" Content="ログイン" Style="{StaticResource PrimaryButton}"
                    Click="LoginButton_Click"/>

            <TextBlock Text="初期パスワード: admin1234" FontSize="11"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       HorizontalAlignment="Center" Margin="0,12,0,0"/>
        </StackPanel>
    </Border>
</Window>
