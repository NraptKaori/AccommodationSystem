<Window x:Class="AccommodationSystem.Views.PaymentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="宿泊税お支払い" Width="560" Height="680"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- タイトル -->
        <TextBlock Grid.Row="0" Text="宿泊税のお支払い" FontSize="22" FontWeight="Bold"
                   Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,20"/>

        <!-- 宿泊情報カード -->
        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,12">
                    <TextBlock Text="宿泊者名" Style="{StaticResource FieldLabel}"/>
                    <TextBlock x:Name="GuestNameText" Style="{StaticResource ValueText}" FontWeight="SemiBold"/>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,12">
                    <TextBlock Text="予約番号" Style="{StaticResource FieldLabel}"/>
                    <TextBlock x:Name="ResNumText" Style="{StaticResource ValueText}"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,12">
                    <TextBlock Text="チェックイン" Style="{StaticResource FieldLabel}"/>
                    <TextBlock x:Name="CheckinText" Style="{StaticResource ValueText}"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,12">
                    <TextBlock Text="チェックアウト" Style="{StaticResource FieldLabel}"/>
                    <TextBlock x:Name="CheckoutText" Style="{StaticResource ValueText}"/>
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,0,12">
                    <TextBlock Text="宿泊人数" Style="{StaticResource FieldLabel}"/>
                    <TextBlock x:Name="PersonsText" Style="{StaticResource ValueText}"/>
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,0,0,12">
                    <TextBlock Text="宿泊泊数" Style="{StaticResource FieldLabel}"/>
                    <TextBlock x:Name="NightsText" Style="{StaticResource ValueText}"/>
                </StackPanel>

                <!-- 税額ハイライト -->
                <Border Grid.Row="3" Grid.ColumnSpan="2"
                        Background="#EBF4FF" CornerRadius="6" Padding="16,12">
                    <Grid>
                        <TextBlock Text="宿泊税合計" FontSize="14" Foreground="{StaticResource PrimaryBrush}"
                                   VerticalAlignment="Center"/>
                        <TextBlock x:Name="TaxAmountText" FontSize="28" FontWeight="Bold"
                                   Foreground="{StaticResource PrimaryBrush}"
                                   HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <!-- 支払い済みの場合のメッセージ -->
        <Border x:Name="AlreadyPaidBorder" Grid.Row="2" Visibility="Collapsed"
                Background="#D1FAE5" CornerRadius="6" Padding="16,12" Margin="0,0,0,16">
            <TextBlock Text="✓ 宿泊税は支払い済みです" FontSize="14" FontWeight="Bold"
                       Foreground="#065F46"/>
        </Border>

        <!-- カード入力エリア -->
        <Border x:Name="PaymentPanel" Grid.Row="3" Style="{StaticResource CardBorder}" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="クレジットカード情報" FontSize="16" FontWeight="Bold"
                           Margin="0,0,0,16" Foreground="{StaticResource TextPrimaryBrush}"/>

                <TextBlock Text="カード番号" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="CardNumberBox" Margin="0,0,0,12"
                     MaxLength="19" Style="{StaticResource PlaceholderTextBox}" Tag="1234 5678 9012 3456"/>

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="有効期限 (MM/YY)" Style="{StaticResource FieldLabel}"/>
                        <TextBox x:Name="ExpBox" MaxLength="5" Style="{StaticResource PlaceholderTextBox}" Tag="MM/YY"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="セキュリティコード" Style="{StaticResource FieldLabel}"/>
                        <PasswordBox x:Name="CvcBox" MaxLength="4"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="カード名義（半角英字）" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="CardNameBox" Style="{StaticResource PlaceholderTextBox}" Tag="TARO YAMADA"/>

                <TextBlock Text="※ 決済はStripeによる安全な処理で行われます"
                           FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,12,0,0"/>
            </StackPanel>
        </Border>

        <!-- ボタン -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="キャンセル"
                    Style="{StaticResource DangerButton}"
                    Click="CancelButton_Click"/>
            <Button x:Name="PayButton" Grid.Column="2" Content="💳  カードで支払う"
                    Style="{StaticResource SuccessButton}"
                    Click="PayButton_Click"/>
        </Grid>
    </Grid>
</Window>
